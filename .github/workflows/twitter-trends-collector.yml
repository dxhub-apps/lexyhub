name: Twitter Trends Collector

on:
  schedule:
    - cron: "0 2 * * *"  # Daily at 2 AM UTC (1 run/day, 50/run = 50/day, 1,500/month)
  workflow_dispatch:
    inputs:
      per_run_budget:
        description: "Tweets to collect this run (default: 50)"
        required: false
        default: "50"

permissions:
  contents: read

jobs:
  collect:
    name: Collect Twitter keyword trends
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: production

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run Twitter collector (with retry)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          TWITTER_BEARER_TOKEN: ${{ secrets.TWITTER_BEARER_TOKEN }}
          TWITTER_MONTHLY_LIMIT: 1500
          TWITTER_PER_RUN_BUDGET: ${{ github.event.inputs.per_run_budget || '31' }}
        run: |
          n=0
          until [ $n -ge 3 ]
          do
            echo "attempt=$((n+1))"
            node scripts/twitter-keyword-collector.mjs && break
            n=$((n+1))
            sleep $((3 * n))
          done
          [ $n -lt 3 ]

      - name: Notify on failure
        if: failure()
        run: echo "Twitter trends collection failed. Check logs for details."
